sudo fwupdmgr update
# check system boottime
systemd-analyze time
# display information of the system
uname -a # for all the stuff
# link bash cmd with config
alias ls="ls --color=auto"
# unalias
mkdir -p {}
#enable a service
sudo systemctl enable --now [service]
# check it status
sudo systemctl status [service]
head and tail -n 5
journalctl
touch [1..3].txt
ln -s create link
find -name -type
chmod chown chgrp umask 
# default file permissions
finger
# neofetch << fastfetch 
# same but with the name indicated the difference
gsconnect if kde connect failed
# debian does not have its bluetooth gui
# gnome blur shell is a joke
tac and cat 
# same but reverse order of display
ncdu # gui disk analyzer
# grep is to find a specific text 
# within the file and can use with other commands
# not fully riceable
# defauft theme is not good looking enough
# to run a train in the terminal
# smb password
# check ip address of the server pc since it can 
# be changed
# kde hates 
# not as lightweight as I thought
# constant crash on the theme shops
# despite being highly customizable, things can get
# buggy and out of place
# this de can get too hot
# the shop is slow

# use & to make multiple cmd 
echo $XDG_SESSION_TYPE #check X11 or wayland

# create customized commands
1. Using alias
example: alias yt='....'
2. Using bash function
example: yt() {
  yt-dlp -f "bv*[ext=mp4]+ba[ext=m4a]/b[ext=mp4]" --merge-output-format mp4 "$@"
}
After that, source .bashrc before using the commands. Another option is using the script should the process is complex.


# packet tracker installation:
Follow this guide: https://github.com/PetrusNoleto/Error-in-install-cisco-packet-tracer-in-ubuntu-23.10-Dependency-is-not-satisfiable-libgl1-mesa-glx?tab=readme-ov-file
Using snap is needed for this installation
